# C Pointer Logic/Arithmetic

- `function_pointer_array.c` - use an array of function pointers. Useful for iterating through functions for testing, for conditionally calling functions tied to indices, etc.
- 